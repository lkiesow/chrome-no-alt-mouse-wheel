{
  "manifest_version": 2,

  "name": "No-Alt-Mouse-Wheel",
  "description": "Prevent mouse wheel scrolling if alt is pressed. This comes in handy if system events are already bound to scrolling with modifier keys like for example the window managers desktop zoom accessibility feature which would otherwise cause page-scrolling in Chrome every time it is being used.",
  "version": "0.1",
  "author": "Lars Kiesow",
  "homepage_url": "https://lkiesow.io",

  "content_scripts": [
    {
      "matches": ["*://*/*"],
      "js": ["no-alt-wheel.js"]
    }
  ],

  "permissions": [
    "activeTab"
  ]
}
